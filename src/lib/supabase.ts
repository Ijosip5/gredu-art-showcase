// Re-exports the generated Lovable Cloud client so every module shares one
// browser session. Do not create a second createClient() instance.
export { supabase } from "@/integrations/supabase/client";
